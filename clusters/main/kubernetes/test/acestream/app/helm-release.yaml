apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
    name: acestream
    namespace: acestream
spec:
    interval: 5m
    chart:
        spec:
            # renovate: registryUrl=https://charts.truechartsoci.org
            chart: acestream
            version: 8.6.1
            sourceRef:
                kind: HelmRepository
                name: truecharts
                namespace: flux-system
            interval: 5m
    install:
        createNamespace: true
        crds: CreateReplace
        remediation:
            retries: 3
    upgrade:
        crds: CreateReplace
        remediation:
            retries: 3
    values:
      image:
        pullPolicy: IfNotPresent
        repository: ghcr.io/vstavrinov/acestream-service
        tag: 2.1.1.1@sha256:3887de78f7ca80e46ea8d80ec846dd20844cec85999610b3ebf6e7af6c19fb48
      persistence:
        acestreamcache:
          enabled: true
          mountPath: /srv/acestream/.ACEStream
        tmp:
          enabled: false
        devshm:
          enabled: false
        varrun:
          enabled: false
      portal:
        open:
          enabled: true
      securityContext:
        container:
          readOnlyRootFilesystem: false
          runAsUser: 0
          runAsGroup: 0
      service:
        main:
          ports:
            main:
              port: 8008
              protocol: tcp
      workload:
        main:
          podSpec:
            containers:
              main:
                env:
                  ENTRY: "{{ .Values.service.main.ports.main.port }}"
                  PORT: "{{ .Values.service.main.ports.main.port }}"
                  SCHEME: http
